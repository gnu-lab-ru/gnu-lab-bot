Стиль кода и работа с LLM

Стиль Elisp
- lexical-binding: t (MUST).
- Префикс имён: gnu-lab- (MUST).
- checkdoc, простые plists, неизменяемые аргументы в pure-функциях (SHOULD).
- Побочные эффекты — только в интерпретаторе эффектов (MUST).

Работа с LLM
- Сначала уточнить контракт в spec/*, затем генерировать код (MUST).
- PR от LLM MUST включать ERT-тесты.
- Нельзя добавлять новые типы Effect/Event без обновления spec/03 и PROTOCOL_VERSION (MUST).
- Безопасность: любые изменения песочницы — обновление spec/04 и тестов (MUST).

Тестирование
- Покрытие core/commands/линтера ≥ 70% (SHOULD).
